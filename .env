
# .env (root of the repository)

# ---- MariaDB ----
MARIADB_ROOT_PASSWORD=Wkc-RPm-5z0-8Pc
MARIADB_DATABASE=driver_safety
MARIADB_USER=afetybonus_user
MARIADB_PASSWORD=FE3-xE5-26J-0QT

# ---- API ----
API_PORT=8080
# DSN used by backend; compose will inject this into the API container
DB_DSN=safety_user:safety_password@tcp(db:3306)/driver_safety?parseTime=true

# ---- Frontend ----
# Used by the React app at build time and by dbStore.ts at runtime (if you wire it).
# Keep this pointing to the backend service port published by compose.
VITE_API_BASE_URL=http://localhost:${API_PORT}/api
